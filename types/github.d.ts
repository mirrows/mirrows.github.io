
export type UserInfo = {
  avatar_url?: string,
  avatarUrl?: string,
  html_url: string,
  login: string,
}

export type PageInfo = {
  endCursor: string,
  startCursor: string,
  hasNextPage: boolean,
  hasPreviousPage: boolean,
}